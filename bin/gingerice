#!/usr/bin/env ruby

$:.unshift(File::join(File::dirname(File::dirname(__FILE__)), "lib"))

require 'gingerice/command'

args = ARGV.dup

# default option
if ARGV.empty?
  args << '-h'
end

command = Gingerice::Command.new(args)
command.execute

